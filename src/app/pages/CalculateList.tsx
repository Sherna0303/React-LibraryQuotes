import { ReactElement } from 'react';
import CopyListContainer from '../containers/CopyListContainer';

export const CalculateList = (): ReactElement => {
  return (
    <>
      <CopyListContainer />
    </>
  );
};