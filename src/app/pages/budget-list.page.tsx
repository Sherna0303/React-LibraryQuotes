import { ReactElement } from 'react';
import { BudgetContainer } from '../containers/budget.container';

export const BudgetListPage = (): ReactElement => {
  return (
    <>
      <BudgetContainer />
    </>
  );
};